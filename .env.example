# SmartCall RPA API 環境変数サンプル
# このファイルを .env にコピーして使用してください
# 注意: 本番の認証情報は絶対にコミットしないでください

# 動作モード（ローカル開発時は stub を設定）
SMARTCALL_MODE=stub

# サーバーポート
PORT=3000

# ログインID/パスワード（開発者ポータルで設定した値）
# デプロイ時は自動設定されるため、ローカル開発時のみ手動設定
LOGIN_KEY=your_login_id
LOGIN_PASSWORD=your_password

# スクリーンショット設定
ENABLE_SCREENSHOT=true
SCREENSHOT_DIR=./screenshots

# Keep-alive間隔（デフォルト: 10分 = 600000ms）
# ※docker-compose.ymlでデフォルト値を設定済み
# ※API実行中はMutex排他制御により競合しないため安全
# KEEP_ALIVE_INTERVAL_MS=600000
